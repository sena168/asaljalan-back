{
  "name": "string-list-backend",
  "version": "1.0.0",
  "description": "Backend API for string list application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "kill-port 5000 && nodemon server.js",
    "kill": "kill-port 5000"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "redis": "^4.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "kill-port": "^2.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}